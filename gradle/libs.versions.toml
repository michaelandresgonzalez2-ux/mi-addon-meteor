[versions]
mod-version = "0.1.0"
jdk = "21"
minecraft = "26.1"
yarn-mappings = "1.21.4+build.8"
fabric-loader = "0.16.9"
loom = "1.4.2"
meteor = "0.5.8-SNAPSHOT"

[libraries]
minecraft = { module = "com.mojang:minecraft", version.ref = "minecraft" }
yarn = { module = "net.fabricmc:yarn", version.ref = "yarn-mappings" }
fabric-loader = { module = "net.fabricmc:fabric-loader", version.ref = "fabric-loader" }
meteor-client = { module = "meteordev:meteor-client", version.ref = "meteor" }

[plugins]
fabric-loom = { id = "net.fabricmc.fabric-loom", version.ref = "loom" }
